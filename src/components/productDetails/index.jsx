import { Layout, Typography } from "antd";

import "./product-details.css";

const { Content } = Layout;
const { Text } = Typography;

export const ProductDetails = () => {
  return <Content className="product-details-container"></Content>;
};
